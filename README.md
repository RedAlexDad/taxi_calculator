# Taxi Calculator

Система расчета стоимости тарифа такси на Python.

Проект разработан как часть лабораторной работы №6 "Тестирование и оценка качества программного проекта" по дисциплине "Технологии разработки программного обеспечения".

## 📋 Описание

**TaxiCalculator** — приложение для динамического расчета стоимости поездки такси с учетом:

- **4 типов тарифов**:
  - Эконом (100 руб/км)
  - Комфорт (150 руб/км)
  - Комфорт+ (200 руб/км)
  - Бизнес (300 руб/км)

- **Факторов, влияющих на цену** (5-балльная шкала):
  - Уровень пробок (коэффициент 1.0 - 2.0)
  - Уровень непогоды (коэффициент 1.0 - 1.5)
  - Уровень спроса/переполненности (коэффициент 1.0 - 2.0)

- **Полная валидация** входных параметров с информативными сообщениями об ошибках
- **Минимальная стоимость поездки** — 50 рублей

## 🚀 Быстрый старт

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/YOUR_USERNAME/taxi-calculator.git
cd taxi-calculator

# Установка зависимостей
pip install -r requirements.txt
```

### Использование

```python
from taxi_calculator import TaxiCalculator

# Создание калькулятора
calc = TaxiCalculator()

# Простой расчет (эконом, 10 км)
fare = calc.calculate_fare(distance=10, tariff='эконом')
print(f"Стоимость: {fare} руб")  # Вывод: Стоимость: 1000.0 руб

# Расчет с факторами (бизнес, 15 км, пробки, дождь, высокий спрос)
fare = calc.calculate_fare(
    distance=15,
    tariff='бизнес',
    traffic_rating=4,      # Тяжелые пробки
    weather_rating=3,      # Дождь
    overload_rating=4      # Высокий спрос
)
print(f"Стоимость: {fare} руб")  # Вывод: Стоимость: 13500.0 руб

# Получение информации о тарифах
info = calc.get_tariff_info()
print(info)
```

## 🧪 Тестирование

### Запуск всех тестов

```bash
# С подробным выводом
python -m unittest test_taxi_calculator -v

# Запуск конкретного теста
python -m unittest test_taxi_calculator.TestTaxiCalculatorValidation.test_validate_distance_positive
```

### Анализ покрытия кода

```bash
# Запуск тестов с анализом покрытия
coverage run -m unittest test_taxi_calculator

# Вывод отчета в консоль
coverage report -m

# Генерация HTML отчета
coverage html

# Открытие HTML отчета
open htmlcov/index.html  # macOS
xdg-open htmlcov/index.html  # Linux
start htmlcov/index.html  # Windows
```

### Статистика тестов

- **Всего тестов**: 30
- **Категории**:
  - Валидация параметров: 11 тестов
  - Расчет стоимости: 10 тестов
  - Граничные случаи: 4 теста
  - Информационные методы: 2 теста
  - Интеграционные: 3 теста

- **Покрытие кода**: 96%
- **Статус**: ✅ Все тесты пройдены

## 📊 Примеры использования

### Пример 1: Базовый расчет

```python
calc = TaxiCalculator()

# Поездка на эконом 10 км при нормальных условиях
fare = calc.calculate_fare(distance=10, tariff='эконом')
assert fare == 1000.0
```

### Пример 2: Учет пробок

```python
# Без пробок
fare_normal = calc.calculate_fare(distance=10, tariff='эконом')
# 10 * 100 * 1.0 = 1000 руб

# С экстремальными пробками (уровень 5)
fare_traffic = calc.calculate_fare(
    distance=10,
    tariff='эконом',
    traffic_rating=5
)
# 10 * 100 * 2.0 = 2000 руб
```

### Пример 3: Комбинированные факторы

```python
fare = calc.calculate_fare(
    distance=20,
    tariff='комфорт_плюс',
    traffic_rating=4,    # Тяжелые пробки (× 1.5)
    weather_rating=3,    # Дождь (× 1.15)
    overload_rating=5    # Экстремальный спрос (× 2.0)
)
# 20 * 200 * 1.5 * 1.15 * 2.0 = 13800 руб
```

### Пример 4: Применение минимума

```python
# Очень короткая поездка (0.1 км)
fare = calc.calculate_fare(distance=0.1, tariff='бизнес')
# Без минимума: 0.1 * 300 = 30 руб
# С минимумом: 50 руб (минимальная стоимость)
assert fare == 50.0
```

## 🏗️ Архитектура

### Структура класса TaxiCalculator

```
TaxiCalculator
├── TARIFF_RATES           # Словарь ставок по тарифам
├── TRAFFIC_MULTIPLIERS    # Коэффициенты пробок
├── WEATHER_MULTIPLIERS    # Коэффициенты непогоды
├── OVERLOAD_MULTIPLIERS   # Коэффициенты спроса
│
├── validate_distance()     # Валидация расстояния
├── validate_tariff()       # Валидация типа тарифа
├── validate_rating()       # Валидация рейтинга (1-5)
├── calculate_fare()        # Расчет стоимости
└── get_tariff_info()       # Получение информации о тарифах
```

### Формула расчета

```
fare = distance × tariff_rate × traffic_multiplier × weather_multiplier × overload_multiplier

fare = max(fare, MIN_FARE)  # Применение минимума
fare = round(fare, 2)        # Округление до копеек
```

## 📝 Документация методов

### calculate_fare()

```python
def calculate_fare(self, distance, tariff, traffic_rating=1, 
                   weather_rating=1, overload_rating=1) -> float:
    """
    Рассчитывает стоимость поездки такси.
    
    Args:
        distance (float): Расстояние поездки в км (> 0)
        tariff (str): Тип тарифа из {'эконом', 'комфорт', 'комфорт_плюс', 'бизнес'}
        traffic_rating (int): Уровень пробок от 1 до 5 (default: 1)
        weather_rating (int): Уровень непогоды от 1 до 5 (default: 1)
        overload_rating (int): Уровень спроса от 1 до 5 (default: 1)
    
    Returns:
        float: Стоимость поездки в рублях
    
    Raises:
        ValueError: Если параметры вне допустимого диапазона
        TypeError: Если параметры неправильного типа
    
    Example:
        >>> calc.calculate_fare(10, 'эконом')
        1000.0
        >>> calc.calculate_fare(15, 'бизнес', traffic_rating=5)
        9000.0
    """
```

### validate_distance()

```python
def validate_distance(self, distance) -> None:
    """Проверяет, что расстояние — положительное число."""
    # Raises: ValueError если distance <= 0
    # Raises: TypeError если distance не число
```

### validate_tariff()

```python
def validate_tariff(self, tariff) -> None:
    """Проверяет, что тариф поддерживается."""
    # Raises: ValueError если тариф не в TARIFF_RATES
```

### validate_rating()

```python
def validate_rating(self, rating, rating_type="") -> None:
    """Проверяет, что рейтинг целое число от 1 до 5."""
    # Raises: ValueError если rating не в [1, 5]
    # Raises: TypeError если rating не целое число
```

### get_tariff_info()

```python
def get_tariff_info() -> dict:
    """
    Возвращает информацию о доступных тарифах.
    
    Returns:
        dict: Словарь с ключами:
            - 'тарифы': список названий тарифов
            - 'ставки': словарь {тариф: ставка}
            - 'минимальная_стоимость': минимальная цена в рублях
    """
```

## 📦 Зависимости

Основная функциональность не требует внешних зависимостей (используется только стандартная библиотека Python).

Для разработки и тестирования требуются:

```
coverage==7.3.2      # Анализ покрытия кода
pytest==7.4.3        # Альтернативный фреймворк тестирования
pytest-cov==4.1.0    # Интеграция покрытия с pytest
```

## 🔍 Профайлинг и оптимизация

### Анализ производительности

```bash
# Профайлинг Python кода
python -m cProfile -s cumtime taxi_calculator.py

# Или через декоратор для отдельных функций
import cProfile
cProfile.run('calc.calculate_fare(100, "эконом")')
```

## 🐛 Обработка ошибок

```python
from taxi_calculator import TaxiCalculator

calc = TaxiCalculator()

# ValueError: неверное значение
try:
    calc.calculate_fare(distance=-5, tariff='эконом')
except ValueError as e:
    print(f"Ошибка: {e}")  # Расстояние должно быть положительным

# TypeError: неверный тип
try:
    calc.calculate_fare(distance="10 км", tariff='эконом')
except TypeError as e:
    print(f"Ошибка: {e}")  # Расстояние должно быть числом

# ValueError: неподдерживаемый тариф
try:
    calc.calculate_fare(distance=10, tariff='премиум')
except ValueError as e:
    print(f"Ошибка: {e}")  # Неподдерживаемый тариф
```

## 🔄 CI/CD интеграция

Проект включает GitHub Actions конфигурацию (`.github/workflows/tests.yml`) для автоматического запуска тестов при каждом коммите и pull request.

```yaml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.8, 3.9, '3.10', '3.11']
```

## 📊 Результаты тестирования

### Сводка по категориям

| Категория | Тестов | Пройдено | Статус |
|-----------|--------|----------|--------|
| Валидация | 11 | 11 | ✅ |
| Расчеты | 10 | 10 | ✅ |
| Граничные случаи | 4 | 4 | ✅ |
| Информация | 2 | 2 | ✅ |
| Интеграция | 3 | 3 | ✅ |
| **ИТОГО** | **30** | **30** | **✅** |

### Покрытие кода

| Модуль | Строк | Покрыто | Процент |
|--------|-------|---------|---------|
| taxi_calculator.py | 120 | 115 | 96% |
| test_taxi_calculator.py | 280 | 280 | 100% |

## 📄 Лицензия

MIT License

## 👥 Автор

Разработано как учебный проект.

Дата: 30 ноября 2025 г.

## 🤝 Вклад

Предложения и исправления приветствуются. Пожалуйста, создавайте pull requests.

## 📞 Контакты

Для вопросов и предложений создавайте Issues на GitHub.

---

**Дополнительная информация:**

- Отчет по лабораторной работе: `docs/report.md`
- Примеры использования: `examples/`
- Документация API: `docs/api.md`
